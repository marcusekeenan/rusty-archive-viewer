// import { PV_COLORS } from "./colors";

// // Define properties for each pen used in the chart
// export type PenProperties = {
//   color: string;
//   opacity: number;
//   lineWidth: number;
//   style: 'solid' | 'dashed' | 'dotted';
//   showPoints: boolean;
//   pointSize: number;
//   displayName?: string;
//   yAxis?: number;
//   visible: boolean;  // New property to control visibility
// };


// // Structure that links each PV to its associated pen properties
// export type PVWithProperties = {
//   name: string;         // Name of the PV
//   pen: PenProperties;   // Visual properties for the PV line
// };

// // Default properties applied to each new pen, using the first color from PV_COLORS
// export const DEFAULT_PEN_PROPERTIES: PenProperties = {
//   color: PV_COLORS[0],
//   opacity: 1,
//   lineWidth: 2,
//   style: 'solid',
//   showPoints: false,
//   pointSize: 4,
//   visible: true,
// };
